/*
 * This settings file was auto generated by the Gradle buildInit task
 * by 'se7kn8' at '09.01.17 21:35' with Gradle 2.14.1
 *
 * The settings file is used to specify which projects to include in your build.
 * In a single project build this file can be empty or even removed.
 *
 * Detailed information about configuring a multi-project build in Gradle can be found
 * in the user guide at https://docs.gradle.org/2.14.1/userguide/multi_project_builds.html
 */

/*
// To declare projects as part of a multi-project build use the 'include' method
include 'shared'
include 'api'
include 'services:webservice'
*/

rootProject.name = 'ServerController'

if(System.getenv("CI")){
    if (System.getenv("BUILD_ANDROID")) {
        //Disabled until .travis.yml is fixed
        //includeApp()
        //includeRest()
    }else{
        includeCore()
        includeRest()
        includeAddons()
    }
}else{
    includeCore()
    includeRest()
    includeApp()
    includeAddons()
}

def includeRest(){
    include 'servercontroller-rest-api'
}

def includeCore(){
    include 'servercontroller-api'
    include 'servercontroller-core'
}

def includeApp(){
    include 'servercontroller-android-app'
}

def includeAddons() {
    include 'vanilla'
    include 'bungeecord'
    include 'craftbukkit'
    include 'spigot'
}